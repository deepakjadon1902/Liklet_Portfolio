{
  "name": "liklet-portfolio",
  "private": true,
  "scripts": {
    "dev": "concurrently -n backend,frontend -c green,cyan \"npm.cmd run dev --prefix backend\" \"npm.cmd run dev --prefix frontend\"",
    "build": "npm.cmd run build --prefix frontend",
    "lint": "npm.cmd run lint --prefix frontend",
    "start": "npm.cmd run start --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.2.0"
  }
}
