# API base URL for the frontend to call the backend.
# In dev, Vite also proxies `/api` -> `http://localhost:5000` (see `vite.config.ts`).
VITE_API_BASE_URL=/api

# Optional FX rate (used for USD display)
# VITE_FX_INR_TO_USD=0.012
